This directory contains all python dependencies outside the default library collection shipped with the python interpreter.

The hntool directory contains the hntool modules see it's won doc for more information.
